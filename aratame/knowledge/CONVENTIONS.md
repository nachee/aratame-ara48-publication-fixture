# Aratame KB conventions v1

Markdown is published synthesis, not original requirements or deployment proof. Manifest distinguishes authored/generated files and immutable file digests. Citation snapshots remain protected in Cloud; a link/hash alone cannot reconstruct source bytes. Test definitions require separate explicit execution approval.
